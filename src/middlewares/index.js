const isAuth = require("./isAuth");
const attachCurrentUser = require("./attachCurrentUser");
const attachUserTorrents = require("./attachUserTorrents");

module.exports = { isAuth, attachCurrentUser, attachUserTorrents };
